# Groww Review Pulse

A Python CLI agent that acts as an MCP host/client to ingest Groww app reviews, summarize them using LLMs, and deliver reports.

## Setup

```bash
# Install dependencies
pip install -e ".[dev]"
```

## Running

```bash
python -m groww_pulse run --help
```
