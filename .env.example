# .env.example
# Copy this file to .env and replace the placeholders with your actual values

# API Key for the LLM Summarizer (Phase 3)
GROQ_API_KEY=gsk_your_groq_api_key_here

# Note: For embeddings, the local BAAI/bge-small-en-v1.5 model is used via sentence-transformers,
# so no API key is required for embeddings.
